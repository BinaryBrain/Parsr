version: '3.3'

services:
  parsr:
    image: axarev/parsr
    ports:
      - 3001:3001
    environment:
      ABBYY_SERVER_URL:
    volumes:
      - ./pipeline/:/tmp

  parsr-ui:
    image: axarev/parsr-ui-localhost
    ports:
      - 8080:80

volumes:
  pipeline:
    driver: local
